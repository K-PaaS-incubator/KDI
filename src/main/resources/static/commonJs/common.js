function bannerTitle (title, subTitle) {
    $('.banner-title').text(title);
    $('.banner-sub-title').text(subTitle);
}

function subTitle (title, navi) {
    $('.main-title-text').text(title);
    $('.navi-arrow').text(navi)
}

function testAlert (title) {
    alert(title);
}