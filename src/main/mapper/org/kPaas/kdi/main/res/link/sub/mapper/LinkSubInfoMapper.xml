<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kPaas.kdi.main.res.link.sub.mapper.LinkSubInfoMapper">

	<insert id="createTable">
		CREATE TABLE `KDI_LINK_SUB_INF` (
		`SVC_LNK_ID` VARCHAR2(100) NOT NULL,
		`SVC_ID` VARCHAR2(50) NOT NULL REFERENCES KDI_LINK_SVC_INF(SVC_ID) ON DELETE CASCADE,
		`SVC_LNK_NM` VARCHAR2(100) NOT NULL,
		`SCH_NM` VARCHAR2(100) NULL,
		`TBL_NM` VARCHAR2(100) NULL,
		`REG_ID` VARCHAR2(20) NULL,
		`REG_DT` DATE NULL,
		`MOD_ID` VARCHAR2(20) NULL,
		`MOD_DT` DATE NULL,
		PRIMARY KEY (`SVC_LNK_ID`)
		)
	</insert>

	<sql id="getLinkPubInfListWhere">
		KDI_LINK_SUB_INF TB1
		JOIN
		KDI_LINK_SVC_INF TB2
		ON
		TB1.SVC_ID = TB2.SVC_ID
		<where>
			TB2.SVC_ID = #{param.value.svcId}
		</where>
	</sql>

	<select id="getListCnt" resultType="Long">
		SELECT
		COUNT(TB1.*)
		FROM
		<include refid="getLinkPubInfListWhere" />
	</select>

	<select id="getList" resultType="Map">
		SELECT
		TB1.SVC_LNK_ID,
		TB1.SVC_ID,
		TB1.SVC_LNK_NM,
		TB1.SCH_NM,
		TB1.TBL_NM,
		TB2.SVC_NM
		FROM
		<include refid="getLinkPubInfListWhere" />
		LIMIT
		#{param.pagePerRow}
		OFFSET
		(#{param.pageNum} - 1) * #{param.pagePerRow}
	</select>

	<insert id="insertPubInterface">
		INSERT INTO KDI_LINK_SVC_INF (
		SVC_LNK_ID,
		SVC_ID,
		SVC_LNK_NM,
		SCH_NM,
		TBL_NM,
		REG_ID,
		REG_DT
		) VALUES (
		#{param.value.svcLnkId},
		#{param.value.svcId},
		#{param.value.svcLnkNm},
		#{param.value.schNm},
		#{param.value.tblNm},
		#{param.value.regId},
		current_timestamp()
		)
	</insert>

	<select id="duplicateCheck" resultType="Integer">
		SELECT count(*) FROM KDI_LINK_SVC_INF
		<where>SVC_LNK_ID = #{param.value.svcLnkId}</where>
	</select>

	<select id="get" resultType="Map">
		SELECT * FROM KDI_LINK_SUB_INF
		<where>SVC_LNK_ID = #{param.value.svcLnkId}</where>
	</select>

	<update id="modify">
		UPDATE KDI_LINK_SUB_INF
		<set>
			SVC_LNK_NM = #{param.value.svcLnkMm},
			MOD_ID = #{param.value.modifyId},
			MOD_DT = current_timestamp()
		</set>
		<where>
			SVC_LNK_ID = #{param.value.svcLnkId}
		</where>
	</update>

	<delete id="delete" parameterType="String">
		DELETE
		FROM
		KDI_LINK_SUB_INF
		<where>
			SVC_LNK_ID = #{param.value.svcLnkId}
		</where>
	</delete>
</mapper>