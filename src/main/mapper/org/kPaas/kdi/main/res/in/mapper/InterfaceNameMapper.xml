<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.kPaas.kdi.main.res.in.mapper.InterfaceNameMapper">

	<insert id="createTable">
		CREATE TABLE `KDI_INTERFACE_NM` (
		`IF_ID` VARCHAR2(100) NOT NULL,
		`IF_NM` VARCHAR2(100) NOT NULL,
		`REG_ID` VARCHAR2(20) NULL,
		`REG_DT` DATE NULL,
		`MOD_ID` VARCHAR2(20) NULL,
		`MOD_DT` DATE
		NULL,
		PRIMARY KEY (`IF_ID`)
		)
	</insert>

	<sql id="getIfNmListWhere">
		KDI_INTERFACE_NM TB1
		<where>
			1=1
			<if test="param.value.ifId != null and param.value.ifId != ''">
				AND TB1.IF_ID LIKE '%' || #{param.value.ifId} || '%'
			</if>
			<if test="param.value.ifNm != null and param.value.ifNm != ''">
				AND TB1.IF_NM LIKE '%' || #{param.value.ifNm} || '%'
			</if>
			<if test="param.value.allSearch != null and param.value.allSearch != ''">
				OR TB1.IF_ID LIKE '%' || #{param.value.ifId} || '%'
				OR TB1.IF_NM LIKE '%' || #{param.value.ifNm} || '%'
			</if>
		</where>
	</sql>
	<select id="getListCnt" resultType="Long">
		SELECT
		COUNT(TB1.*)
		FROM
		<include refid="getIfNmListWhere" />
	</select>

	<select id="getList" resultType="Map">
		SELECT
		TB1.IF_ID,
		TB1.IF_NM
		FROM
		<include refid="getIfNmListWhere" />
		ORDER BY TB1.IF_ID, TB1.IF_NM
		LIMIT #{param.pagePerRow}
		OFFSET (#{param.pageNum} - 1) * #{param.pagePerRow}
	</select>

	<insert id="insert">
		INSERT INTO KDI_INTERFACE_NM
		(
		IF_ID,
		IF_NM,
		REG_ID,
		REG_DT
		)
		VALUES
		(
		#{param.value.ifId},
		#{param.value.ifNm},
		#{param.value.regId},
		current_timestamp()
		)
	</insert>

	<select id="duplicateCheck" resultType="Integer">
		SELECT count(*) FROM KDI_INTERFACE_NM
		<where>IF_ID = #{param.value.ifId}</where>
	</select>

	<select id="get" resultType="Map">
		SELECT * FROM KDI_INTERFACE_NM
		<where>
			IF_ID = #{param.value.ifId}
		</where>
	</select>

	<update id="modify">
		UPDATE KDI_INTERFACE_NM
		<set>
			IF_ID = #{param.value.ifId},
			IF_NM = #{param.value.ifNm},
			REG_ID = #{param.value.modifyId},
			REG_DT = current_timestamp()
		</set>
		<where>
			IF_ID = #{param.value.orgIfId}
		</where>
	</update>

	<delete id="delete">
		DELETE
		FROM
		KDI_INTERFACE_NM
		<where>
			IF_ID = #{param.value.ifId}
		</where>
	</delete>
</mapper>